/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
    domains: ['randomuser.me', 'p7.hiclipart.com','images.unsplash.com','res.cloudinary.com'],
  },
};

export default nextConfig;
